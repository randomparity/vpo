# Single Phase Policy Fixture
# Used to test basic phased schema parsing and single phase execution
schema_version: 12

config:
  audio_language_preference: [eng, und]
  subtitle_language_preference: [eng]
  on_error: continue

phases:
  - name: normalize
    audio_filter:
      languages: [eng, und]
      minimum: 1
    subtitle_filter:
      languages: [eng, und]
      preserve_forced: true
