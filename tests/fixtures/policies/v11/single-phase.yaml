# V11 Single Phase Policy Fixture
# Used to test basic V11 schema parsing and single phase execution
schema_version: 11

config:
  audio_language_preference: [eng, und]
  subtitle_language_preference: [eng]
  on_error: continue

phases:
  - name: normalize
    audio_filter:
      languages: [eng, und]
      minimum: 1
    subtitle_filter:
      languages: [eng, und]
      preserve_forced: true
