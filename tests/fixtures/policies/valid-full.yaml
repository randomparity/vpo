schema_version: 12

config:
  audio_language_preference:
    - eng
    - jpn
    - spa
  subtitle_language_preference:
    - eng
    - spa

phases:
  - name: transcode
    transcode:
      video:
        target_codec: hevc

  - name: transcribe
    transcription:
      enabled: true
